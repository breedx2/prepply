<% if(typeof lightbox !== 'undefined') { %>

<script type="text/javascript" src="/js/jquery.fancybox.min.js"></script>

<script type="text/javascript">
$('.lightbox a img').each((i, elem) => {
  // fancybox really loves to have this attr on elements for gallery to work.
  // perhaps goofy to jump thru parent, but prevents selecting anchors with no image inside
  $(elem).parent().attr('data-fancybox', 'gallery');
});
// console.log($('.lightbox a[data-fancybox=gallery]'));
$('.lightbox a[data-fancybox=gallery]').fancybox({
  wheel: 'auto',
  transitionEffect: 'circular',
  loop: true
});
</script>

<% } %>
